name: Deploy Pages
on:
  push:
    branches: [main]
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Normalize image URLs
        shell: python
        run: |
          from pathlib import Path
          import re
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          raw = 'https://raw.githubusercontent.com/yusuketurner/juk/main/'
          cdn = 'https://cdn.jsdelivr.net/gh/yusuketurner/juk@main/'
          s = s.replace(raw, cdn)
          # Repair the malformed encoded path used once in the second gallery row.
          s = re.sub(
              r'https://cdn\.jsdelivr\.net/gh/yusuketurner/juk@main/[^\"\']*22_22_24\.png',
              cdn + 'ChatGPT%20Image%202026%E5%B9%B49%E6%9C%8810%E6%97%A5%2022_22_24.png',
              s,
          )
          p.write_text(s, encoding='utf-8')
          urls = re.findall(r'https://cdn\.jsdelivr\.net/gh/yusuketurner/juk@main/[^\"\']+', s)
          if not urls:
            raise SystemExit('No CDN image URLs found')
          if 'raw.githubusercontent.com/yusuketurner/juk/main/' in s:
            raise SystemExit('Raw GitHub image URL remains')
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
